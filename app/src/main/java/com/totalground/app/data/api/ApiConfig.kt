package com.totalground.app.data.api

object ApiConfig {
    /**
     * Single configurable BASE_URL for production backend API.
     * Change this constant to point to your live domain or staging server.
     */
    const val BASE_URL = "https://www.totalground.com/"
}
